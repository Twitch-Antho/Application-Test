const canvas=document.getElementById('game'),ctx=canvas.getContext('2d');const scoreEl=document.getElementById('score'),levelEl=document.getElementById('level'),bestEl=document.getElementById('bestScore'),pointsEl=document.getElementById('points'),overlay=document.getElementById('overlay'),title=document.getElementById('overlayTitle'),text=document.getElementById('overlayText'),button=document.getElementById('startBtn'),statusText=document.getElementById('statusText'),rewardText=document.getElementById('rewardText');const W=700,H=500;let running=false,score=0,level=1,last=0,spawn=0,raf=0,earnedLevels=0;let player={x:350,y:440,speed:390};let rocks=[],keys={left:false,right:false};let best=Number(localStorage.getItem('neon-best')||0),points=Number(localStorage.getItem('neon-points')||0),difficulty='normal';const settings={easy:{label:'Facile',reward:.5,spawn:.88,speed:.78},normal:{label:'Normal',reward:1,spawn:.68,speed:1},hard:{label:'Difficile',reward:2,spawn:.43,speed:1.3}};bestEl.textContent=best;pointsEl.textContent=points;const dButtons=document.querySelectorAll('.difficulty');dButtons.forEach(b=>b.addEventListener('click',()=>{if(running)return;difficulty=b.dataset.difficulty;dButtons.forEach(x=>x.classList.remove('active'));b.classList.add('active');rewardText.textContent=`Récompense : +${settings[difficulty].reward} pt / niveau`}));function resize(){const d=devicePixelRatio||1;canvas.width=W*d;canvas.height=H*d;ctx.setTransform(d,0,0,d,0,0)}resize();addEventListener('resize',resize);function reset(){score=0;level=1;earnedLevels=0;rocks=[];player.x=W/2;scoreEl.textContent=0;levelEl.textContent=1}function start(){reset();running=true;overlay.classList.add('hidden');statusText.textContent='EN JEU';last=performance.now();raf=requestAnimationFrame(loop)}function end(){running=false;cancelAnimationFrame(raf);if(score>best){best=Math.floor(score);localStorage.setItem('neon-best',best);bestEl.textContent=best}title.textContent='PARTIE TERMINÉE';text.innerHTML=`Score final : <strong style="color:#55f5df">${Math.floor(score)}</strong><br />Tu as gagné ${earnedLevels} point${earnedLevels>1?'s':''}.`;button.innerHTML='REJOUER <span>→</span>';overlay.classList.remove('hidden');statusText.textContent='PERDU'}function meteor(){const size=12+Math.random()*18,s=settings[difficulty];rocks.push({x:size+Math.random()*(W-size*2),y:-size,size,vy:(125+level*17+Math.random()*80)*s.speed,rot:Math.random()*7})}function update(dt){const s=settings[difficulty];if(keys.left)player.x-=player.speed*dt;if(keys.right)player.x+=player.speed*dt;player.x=Math.max(22,Math.min(W-22,player.x));spawn-=dt;if(spawn<=0){meteor();spawn=Math.max(.18,s.spawn-level*.035)}rocks.forEach(r=>{r.y+=r.vy*dt;r.rot+=dt*2});rocks=rocks.filter(r=>r.y<H+40);for(const r of rocks){if(Math.abs(r.x-player.x)<r.size+14&&Math.abs(r.y-player.y)<r.size+12){end();return}}score+=dt*10;const next=Math.floor(score/100)+1;if(next!==level){level=next;levelEl.textContent=level;points+=s.reward;earnedLevels+=s.reward;pointsEl.textContent=points;localStorage.setItem('neon-points',points)}scoreEl.textContent=Math.floor(score)}function draw(){ctx.clearRect(0,0,W,H);ctx.fillStyle='#080d22';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#152047';for(let x=0;x<W;x+=50){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke()}for(let y=0;y<H;y+=50){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}for(let i=0;i<35;i++){const x=i*137%W,y=(i*79+score*2)%H;ctx.fillStyle=i%3?'#43517a':'#55f5df';ctx.globalAlpha=.45;ctx.fillRect(x,y,2,2)}ctx.globalAlpha=1;rocks.forEach(r=>{ctx.save();ctx.translate(r.x,r.y);ctx.rotate(r.rot);ctx.shadowBlur=18;ctx.shadowColor='#ff4d8d';ctx.fillStyle='#ff4d8d';ctx.beginPath();for(let i=0;i<7;i++){const a=i*Math.PI*2/7,rad=i%2?r.size*.65:r.size;ctx.lineTo(Math.cos(a)*rad,Math.sin(a)*rad)}ctx.closePath();ctx.fill();ctx.restore()});ctx.save();ctx.translate(player.x,player.y);ctx.shadowBlur=20;ctx.shadowColor='#55f5df';ctx.fillStyle='#55f5df';ctx.beginPath();ctx.moveTo(0,-17);ctx.lineTo(16,13);ctx.lineTo(0,8);ctx.lineTo(-16,13);ctx.closePath();ctx.fill();ctx.fillStyle='#101832';ctx.beginPath();ctx.arc(0,0,4,0,Math.PI*2);ctx.fill();ctx.restore()}function loop(now){if(!running)return;const dt=Math.min((now-last)/1000,.05);last=now;update(dt);draw();if(running)raf=requestAnimationFrame(loop)}addEventListener('keydown',e=>{if(e.key==='ArrowLeft'||e.key.toLowerCase()==='a')keys.left=true;if(e.key==='ArrowRight'||e.key.toLowerCase()==='d')keys.right=true;if(e.key===' '&&!running)start()});addEventListener('keyup',e=>{if(e.key==='ArrowLeft'||e.key.toLowerCase()==='a')keys.left=false;if(e.key==='ArrowRight'||e.key.toLowerCase()==='d')keys.right=false});canvas.addEventListener('pointermove',e=>{if(running){const r=canvas.getBoundingClientRect();player.x=(e.clientX-r.left)/r.width*W}});canvas.addEventListener('pointerdown',e=>{if(running){const r=canvas.getBoundingClientRect();player.x=(e.clientX-r.left)/r.width*W}});button.addEventListener('click',start);draw();
